import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'dicionario_biografico',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
